# /etc/sysctl.d/99-ip-forward.conf
# Required for Tailscale subnet routing.
# Applied automatically on boot; or run: sudo sysctl -p /etc/sysctl.d/99-ip-forward.conf

net.ipv4.ip_forward = 1
net.ipv6.conf.all.forwarding = 1
